#include<stdio.h>
struct point
{
    int x;
    int y;
}a,b;


int main{
    printf("Enter the first point: ");
    scanf("%d %d",&a.x,&a.y);
    printf("Enter the second point: ");
    scanf("%d %d",&b.x,&b.y);

    printf("distance=%f", )
}